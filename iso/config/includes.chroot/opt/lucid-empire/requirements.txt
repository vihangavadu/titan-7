# LUCID EMPIRE TITAN V7.0 - Python Dependencies (Unified Edition)
# ================================================================
# Architecture: Sovereign ISO + Full Zero Detect + Camoufox
# - Kernel-level masking (eBPF/XDP + LKM + LD_PRELOAD)
# - Application-level anti-detection (JA4, Canvas, Ghost Motor)
# - Camoufox anti-detect browser + vanilla Firefox ESR / Chromium
# - Genesis Engine + Direct SQLite Injection + LSNG

# GUI Framework
PyQt6>=6.5.0
PyQt6-WebEngine>=6.5.0
qt-material>=2.14

# Anti-Detect Browser Engine
camoufox[geoip]>=0.4.11
browserforge>=1.0.0

# Browser Automation (Genesis Engine profile building)
playwright>=1.40.0

# Web Framework (Backend API & Console)
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0
flask>=3.0.0
flask-cors>=4.0.0
Jinja2>=3.1.0

# Cryptography & Security
cryptography>=41.0.0
pycryptodome>=3.19.0

# Data Handling & Compression
python-snappy>=0.6.1
orjson>=3.9.0
lz4>=4.3.0

# Network & HTTP
httpx>=0.25.0
aiohttp>=3.9.0
PySocks>=1.7.1
requests>=2.31.0

# Database
aiosqlite>=0.19.0

# System & Process
psutil>=5.9.0
watchdog>=3.0.0

# Time & Timezone
astral>=3.2
pytz>=2023.3

# Monitoring
prometheus_client>=0.17.0

# Rate Limiting (API)
slowapi>=0.1.8

# Data Validation
typing_extensions>=4.5.0

# eBPF (optional - requires kernel support)
# bcc>=0.28.0

# Utilities
python-dateutil>=2.8.2
humanize>=4.8.0
pyyaml>=6.0

# Image Processing (for canvas noise)
Pillow>=10.0.0
numpy>=1.24.0

# Perlin Noise (deterministic canvas/webgl fingerprints)
noise>=1.2.2
